# Given a string s, you need to slice it so that the output is a substring that contains all 
# the characters except first and last. The length of the s will be greater than 2.
s = "Hello"
print(s[1:-1])
